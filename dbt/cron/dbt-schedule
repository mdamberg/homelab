# 1 Define environemt variables

# 2. Schedule the cron jobs
# ┌───────────── minute (0 - 59)
# │ ┌───────────── hour (0 - 23)
# │ │ ┌───────────── day of month (1 - 31)
# │ │ │ ┌───────────── month (1 - 12)
# │ │ │ │ ┌───────────── day of week (0 - 6) (Sunday to Saturday)
# │ │ │ │ │
# * * * * * <command to execute>

# Run q 2 hours starting at 6 am
0 6-22/2 * * *  dbt run && dbt test

0 0   * * *  dbt source freshness && dbt run --full-refresh --select path:models/staging && dbt test
